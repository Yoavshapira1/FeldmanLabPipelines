# 3rd Phase - To Epochs
- `raw_to_epochs.py`: Script to transform raw data into epochs. 
This script processes the raw EEG data files in .fif format, creating fixed-length epochs with no baseline correction and saves the resulting EpochsArray as epochs .fif files with '_epo.fif' suffix.